---
listing: 
  contents: constitution # all Quarto docs in the `posts` directory will be included on yourlistingpage
  type: grid # or `default` or `table`; each type has its own set of yaml options to include
  fields: [author,title,subtitle,date]
  sort: "title desc" # can also sort on more than one field
  categories: false # allows you to sort posts by assigned categories 
page-layout: article
---

:::{.box .bs-right}

# The Second Republic
:::